import styled from "styled-components";

const MenuImg = styled.div`
    display: flex;
    justify-content: end;
    flex-direction: row;
    margin-top: 60px;
    margin-right: 10px;
`;

export default MenuImg